name: BMS CI/CD Pipeline (Ubuntu Latest)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: bms-app
      CONTAINER_NAME: bms-app
      APP_PORT: 3000

    steps:
      # 1️⃣ Checkout code
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3️⃣ Install dependencies
      - name: Install Dependencies
        run: |
          npm install
          chmod -R 755 node_modules/.bin

      # 4️⃣ Install Trivy & Pull OWASP ZAP
      - name: Install Trivy & Pull ZAP Image
        run: |
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin
          docker pull zaproxy/zap-stable:latest

      # 5️⃣ Download Trivy HTML template
      - name: Download Trivy HTML Template
        run: |
          curl -sL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/html.tpl -o html.tpl

      # 6️⃣ Trivy Filesystem Scan + HTML Report
      - name: Trivy Filesystem Scan
        run: |
          trivy fs --exit-code 0 --severity HIGH,CRITICAL -f template -o trivy-fs-report.html --template ./html.tpl .

      # 7️⃣ Build React App
      - name: Build React App
        run: CI=false npm run build

      # 8️⃣ Build Docker Image
      - name: Build Docker Image
        run: docker build -t $IMAGE_NAME .

      # 9️⃣ Trivy Docker Image Scan + HTML Report
      - name: Trivy Docker Image Scan
        run: |
          trivy image --exit-code 0 --severity HIGH,CRITICAL -f template -o trivy-image-report.html --template ./html.tpl $IMAGE_NAME

      # 🔟 Stop & Remove Existing Container
      - name: Stop & Remove Old Container
        run: |
          if [ $(docker ps -a -q -f name=$CONTAINER_NAME) ]; then
            docker rm -f $CONTAINER_NAME
          else
            echo "No old container found"
          fi

      # 1️⃣1️⃣ Run Docker Container
      - name: Run Docker Container
        run: |
          docker run -d -p $APP_PORT:$APP_PORT --name $CONTAINER_NAME $IMAGE_NAME
          sleep 15

      # 1️⃣2️⃣ OWASP ZAP DAST Scan + HTML Report
      - name: DAST Scan (OWASP ZAP)
        run: |
          docker run --rm --network host zaproxy/zap-stable zap-baseline.py \
            -t http://localhost:$APP_PORT \
            -r zap_report.html || true

      # 1️⃣3️⃣ Upload Trivy Reports
      - name: Upload Trivy Reports
        uses: actions/upload-artifact@v4
        with:
          name: trivy-reports
          path: |
            trivy-fs-report.html
            trivy-image-report.html

      # 1️⃣4️⃣ Upload ZAP Report
      - name: Upload ZAP Report
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: zap_report.html

      # 1️⃣5️⃣ Smoke Test
      - name: Smoke Test
        run: |
          curl -f http://localhost:$APP_PORT || exit 1
